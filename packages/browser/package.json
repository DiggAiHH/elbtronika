{
  "name": "@elbtronika/browser",
  "description": "EXPERIMENTAL: self-healing Playwright browser harness for agents. Not imported by any app yet — kept for the agent roadmap (see PLAN_RESTARBEITEN §5 'Danach'). packages/config and packages/sanity-studio were removed as dead in Sprint 5.",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./harness": "./src/harness.ts",
    "./cdp": "./src/cdp.ts",
    "./tools": "./src/tools.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@elbtronika/mcp": "workspace:*",
    "playwright": "^1.61.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
