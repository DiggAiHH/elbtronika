{
  "name": "@elbtronika/logger",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Isomorphic structured logger for shared packages (browser + node). Same API as apps/web src/lib/logger.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
