# Deploy-Log: 2026-04-30 v1.1.0

## Zusammenfassung
Go-Live Vorbereitung: Lighthouse, Penetration-Test, Staging/Production Deploy, 48h Monitoring.

## Changes
- Lighthouse CI Workflow (`.github/workflows/lighthouse.yml`)
- OWASP ZAP Security Scan Workflow (`.github/workflows/zap-scan.yml`)
- Staging Deploy Workflow (`.github/workflows/staging-deploy.yml`)
- Production Deploy Workflow (`.github/workflows/production-deploy.yml`)
- Monitoring Dashboard (`/dashboard/monitoring`)
- 48h Monitoring Script (`scripts/monitoring/48h-check.js`)
- Local Lighthouse Script (`scripts/lighthouse-local.js`)
- ZAP Rules Konfiguration (`.zap/rules.tsv`)
- Proxy Edge Function (`netlify/edge-functions/proxy.ts`)

## GitHub Actions Workflows

### Lighthouse CI
- Trigger: push to main/staging, PRs, manual
- Prüft: Performance ≥ 90, Accessibility ≥ 95, Best Practices ≥ 95, SEO = 100
- LCP < 2.0s, CLS < 0.05, TBT < 200ms

### OWASP ZAP
- Baseline Scan: wöchentlich Sonntags 02:00 UTC
- API Scan: manuell triggerbar
- Ignoriert bekannte False Positives (CSP frame-ancestors, Netlify Headers)

### Staging Deploy
- Environment: `staging` → `https://staging.elbtronika.art`
- Pre-deploy: typecheck, lint, test, audit
- Post-deploy: Smoke Tests (Health, Pages, API)
- Slack Notifications

### Production Deploy
- **Manuelle Bestätigung erforderlich** (`DEPLOY` eingeben)
- Gates: Lighthouse + Security müssen passen
- Environment: `production` → `https://elbtronika.art`
- Post-deploy: Health, Pages, Security Headers, SSL
- Auto-Release auf GitHub
- Erstellt 48h Monitoring Issue

## Secrets (erforderlich)

| Secret | Verwendung |
|--------|-----------|
| `NETLIFY_AUTH_TOKEN` | Netlify CLI Auth |
| `NETLIFY_SITE_ID` | Production Site |
| `NETLIFY_STAGING_SITE_ID` | Staging Site |
| `PROD_SUPABASE_URL` | Production DB |
| `PROD_SUPABASE_ANON_KEY` | Production DB |
| `STAGING_SUPABASE_URL` | Staging DB |
| `STAGING_SUPABASE_ANON_KEY` | Staging DB |
| `PROD_STRIPE_PUBLISHABLE_KEY` | Production Stripe |
| `PROD_STRIPE_SECRET_KEY` | Production Stripe |
| `STAGING_STRIPE_PUBLISHABLE_KEY` | Staging Stripe |
| `STAGING_STRIPE_SECRET_KEY` | Staging Stripe |
| `SLACK_DEPLOY_WEBHOOK` | Deploy Notifications |
| `LHCI_GITHUB_APP_TOKEN` | Lighthouse PR Comments |

## Post-Deploy-Checks
- [ ] `/api/health` liefert 200
- [ ] `/dashboard/monitoring` erreichbar
- [ ] Lighthouse Gate passed
- [ ] ZAP Scan keine Critical/High Findings
- [ ] 48h Monitoring Issue erstellt

## Monitoring
- 48h Intensiv-Monitoring nach Go-Live
- Automatische Checks alle 30 Minuten via `scripts/monitoring/48h-check.js`
- Alerts bei Health-Check-Fehlern
- Web Vitals Dashboard beobachten
