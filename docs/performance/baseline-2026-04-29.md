# Performance & Security Baseline — 29.04.2026

## Test-Status
- **Unit Tests:** 7/7 Packages passing (apps/web: 24 tests)
- **Typecheck:** 9/9 Packages passing (mit NODE_OPTIONS=--max-old-space-size=4096)

## Security Audit
- **Vulnerabilities:** 3 moderate (uuid@8.3.2 via Sanity — Upstream blockiert)
- **Critical/High:** 0 ✅

## Build-Zeit
- **Gesamt:** ~3m40s (nach OOM-Fix + typedRoutes-Entfernung + Suspense-Boundary-Fix)
- **Status:** ✅ Erfolgreich (38/38 Seiten + API-Routen)

## Lighthouse
- **Noch zu messen** (Phase 0.1.1 — erfordert laufenden Server)

## Bundle
- **First Load JS shared:** 102 kB
- **Chunk 047a343b:** 54.2 kB
- **Chunk 1194-fd0299c:** 45.7 kB
- **Noch zu analysieren** (Phase 0.1.3 — @next/bundle-analyzer)

## Web Vitals
- **Noch zu implementieren** (Phase 0.1.4)

## Fixes während Phase 0
- `turbo.json`: `globalPassThroughEnv` + `env.NODE_OPTIONS` für OOM-Fix
- `packages/ui/src/index.ts`: `.js` Extensions aus Imports entfernt
- `packages/ui/src/components/*`: Alle `.js` Imports gefixt
- `apps/web/next.config.ts`: `typedRoutes` deaktiviert (i18n dynamische Routen nicht kompatibel)
- `apps/web/next.config.ts`: `reactCompiler` entfernt (in 15.5.15 nicht als Top-Level-Key gültig)
- `apps/web/next.config.ts`: `output: "standalone"` lokal deaktiviert (Windows Symlink-Berechtigungen)
- `artist-onboarding/stripe/page.tsx` + `login/page.tsx`: `<Suspense>` Boundary für `useSearchParams()`
- `gallery/page.tsx`: `export const dynamic = "force-dynamic"` (Sanity-Daten nicht zur Build-Zeit verfügbar)
- `.env.local` mit Dummy-Werten für Build-Test erstellt

---
*Baseline erstellt am: 29.04.2026*
